image: debian:9

stages:
  - compile

Install dependencies:
  stage: compile
  script:
   - apt-get update -qy
   - apt-get install -y build-essential libsqlite3-dev

make:
  stage: compile
  script:
   - make
